version: '2'
services:
  remote-storage:
    image: minio/minio:latest
    ports:
    - "9000:9000"
    command: server /data/
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123

